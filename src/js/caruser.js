const sale = [
    {name: "BAS.Бухгалтерія 2.0",price: 6900.00},
    {name: "BAS.Малий бізнес",price: 6900.00}
];

const domContainer = document.querySelector('#reactapp');
const root = ReactDOM.createRoot(domContainer);

   
const Good = ({ name, price}) => {
    return (
    <div>
        <p>{name}</p>
        <></>
    </div>)
    };


export function carusel (goods) {

} 

root.render(e(LikeButton));